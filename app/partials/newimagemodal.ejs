<div class="modal fade" id="newImageModal" tabindex="-1">
	<div class="modal-dialog">
		<div class="modal-content" data-bind="with: editingImage">
			<div class="modal-header">
				Adding a new image
			</div>
			<div class="modal-body">
				<p>Reminder: add option to upload image and read height, width & name from it.</p>
				<p>Name:</p>
				<input type="text" data-bind="value: name">
				<p>Width:</p>
				<input type="number" data-bind="value: size.width">
				<p>Height:</p>
				<input type="number" data-bind="value: size.height">
			</div>
			<div class="modal-footer">
				<button class="btn btn-default" data-dismiss="modal">Close</button>
				<button class="btn btn-primary" data-bind="click: function() { 
					console.log(JSON.parse(ko.toJSON(this, null, 2)));
				}">Debug</button>
				<button class="btn btn-primary" data-bind="click: function() { $root.addImage(JSON.parse(ko.toJSON(this, null, 2))); }" data-dismiss="modal">Save changes</button>
			</div>
		</div>
	</div>
</div>
